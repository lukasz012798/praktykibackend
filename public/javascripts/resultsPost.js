const addResultButton = document.querySelector("input[type='submit']");
const form = document.querySelector("form");

addNewsButton.addEventListener("click", (e) => {
  e.preventDefault();
  alert("xd");
  console.log("works");
});
